# Blockly Gateway 配置文件

server:
  host: "127.0.0.1"
  port: 9406

gateway:
  heartbeat_check_interval: 10  # 心跳检测间隔（秒）
  heartbeat_timeout: 25         # 心跳超时（秒）

log:
  level: "debug"    # 日志级别: debug, info, warn, error
  format: "text"    # 日志格式: json, text
